import { ImageGalleryDirective } from './image-gallery.directive';

describe('ImageGalleryDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageGalleryDirective();
    expect(directive).toBeTruthy();
  });
});
